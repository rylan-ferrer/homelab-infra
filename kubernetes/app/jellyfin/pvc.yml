apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jellyfin-pvc 
  namespace: default
spec:
  storageClassName: nfs-client
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Gi