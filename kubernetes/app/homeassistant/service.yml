apiVersion: v1
kind: Service
metadata:
  name: homeassistant-svc
  namespace: default
  annotations:
    metallb.io/loadBalancerIPs: 192.168.0.216
spec:
  type: LoadBalancer
  selector: 
    app: homeassistant
  ports:
    - port: 80
      targetPort: 8123
      name: http
      protocol: TCP